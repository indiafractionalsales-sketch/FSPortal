---
name: Build Project
description: Workflow to install dependencies, compile, and build the NextJS project
---

# Build Project

1. Install dependencies:
   ```bash
   npm install
   ```

2. Typecheck (optional):
   ```bash
   npm run typecheck
   ```

3. Build the project:
   ```bash
   npm run build
   ```
